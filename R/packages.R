# map
require(sf)
require(here)
require(rnaturalearth)
require(rgdal)
require(rgeos)
require(maptools)
require(raster)

# plot
require(tidyverse)
require(vctrs)
require(ggplot2)
require (reshape)
require(gridExtra)
library(ggplot2)
library(gridExtra)
library(grid)
require(ggrepel)
require(openxlsx)
require(dplyr)


# elevation
require(elevatr)
# maps
library(ggnewscale)
library(stars)
